export const root = "/"
export const loginSlug = "login"
export const registerSlug = "register"
export const homeSlug = "home"

export const homePath = `${root}${homeSlug}`
export const loginPath = `${root}${loginSlug}`
export const registerPath = `${root}${registerSlug}`